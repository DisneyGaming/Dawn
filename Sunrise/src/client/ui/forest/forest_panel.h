#pragma once

namespace sunrise::client::ui::forest {

/** Draws the Infinite Forest generator dial inside the active Core UI frame. */
void draw() noexcept;

} // namespace sunrise::client::ui::forest
